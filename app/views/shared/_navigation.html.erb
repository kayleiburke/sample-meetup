<nav class="navbar navbar-inverse bg-primary">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Meetup</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% menu_items.each do |menu_item| %>
        <li class="<%= current_page?(polymorphic_path(menu_item)) ? 'nav-item active' : 'nav-item'%>">
          <%= link_to menu_item.to_s.pluralize, polymorphic_path(menu_item), :class => 'nav-link primary'  %>
        </li>
        <% end if defined? menu_items %>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if !current_user.present? %>
            <li class="nav-item">
              <%= link_to "Login", new_user_session_path, class: "btn btn-outline-light me-2" %>
            </li>
            <li class="nav-item">
              <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-outline-light me-2" %>
            </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Welcome #{current_user.email}", "#", class: "btn btn-outline-light me-2", disabled: true %>
          </li>
          <li class="nav-item">
            <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "btn btn-outline-light me-2" %>
          </li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
