<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container">
    <a class="navbar-brand" href="/">Meetup</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <% menu_items.each do |menu_item| %>
          <li class="<%= current_page?(polymorphic_path(menu_item)) ? 'nav-item active' : 'nav-item'%>">
            <%= link_to menu_item.to_s.pluralize, polymorphic_path(menu_item), :class => 'nav-link'  %>
          </li>
        <% end if defined? menu_items %>
      </ul>
    </div>
  </div>
</nav>
