<% if notice %>
  <%= render "shared/notice", notice: notice %>
<% end %>

<h2>Group Info</h2>

<%= render 'form', group: @group %>

<%= render 'engagements', group: @group %>

<%= render 'shared/back_button', path: groups_path %>

